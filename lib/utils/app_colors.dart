import 'package:flutter/material.dart';

class AppColors {
  // Tema Gelap yang Elegan
  static const Color primaryBackground =
      Color(0xFF1A1A2E); // Biru malam yang sangat gelap
  static const Color cardBackground =
      Color(0xFF16213E); // Sedikit lebih terang untuk kartu
  static const Color accentColor =
      Color(0xFFE94560); // Aksen merah muda/merah yang berani
  static const Color secondaryAccent = Color(0xFF0F3460); // Aksen biru tua
  static const Color headlineText =
      Color(0xFFFFFFFF); // Putih cerah untuk kontras
  static const Color bodyText =
      Color(0xFFA0AEC0); // Abu-abu muda untuk teks sekunder
  static const Color subtleShadow = Colors.black26;
  static const Color iconColor = Color(0xFFFFFFFF);
  static const Color iconActiveColor = accentColor;

  // Anda juga bisa mendefinisikan palet terang jika ingin ada opsi ganti tema
  // static const Color lightPrimaryBackground = Color(0xFFF0F2F5);
  // static const Color lightCardBackground = Color(0xFFFFFFFF);
  // static const Color lightAccentColor = Color(0xFF5A67D8); // Indigo untuk tema terang
  // dst.

  // Tambahkan warna lain sesuai kebutuhan
  // Misalnya, untuk error, warning, success, dll.
  static const Color errorColor = Color(0xFFB00020); // Merah untuk error
  static const Color warningColor =
      Color(0xFFFFA500); // Kuning untuk peringatan
  static const Color successColor = Color(0xFF28A745); // Hijau untuk sukses

  // Anda bisa menambahkan lebih banyak warna sesuai kebutuhan aplikasi
  // Misalnya, untuk background loading, border, dll.
  static const Color loadingBackground =
      Color(0xFF121212); // Gelap untuk background loading
  static const Color borderColor =
      Color(0xFFCCCCCC); // Abu-abu terang untuk border
}
